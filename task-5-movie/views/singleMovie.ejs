<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= singleMovie.title %> - Details
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100">

    <div class="container mx-auto py-10">
        <a href="/movie" class="text-blue-500 hover:underline mb-6 block">← Back to all Movies List</a>

        <div class=" rounded-lg max-w-[1000px] mx-auto overflow-hidden flex flex-col md:flex-row">
            <div class="md:w-2/3 p-4">
                <img src="<%= singleMovie.movieImage %>" alt="<%= singleMovie.title %>" class="w-full h-[400px] object-cover rounded">
                <!-- <img src=https://images.pexels.com/photos/5662857/pexels-photo-5662857.png?auto=compress&cs=tinysrgb&w=600"
                    alt="<%= singleMovie.title %>" class="w-full h-auto object-cover rounded"> -->
            </div>

            <div class="md:w-1/2 p-6">
                <div class="flex-col content-center">
                    <h1 class="text-3xl font-bold text-gray-800 mb-4">
                        <%= singleMovie.title %>
                    </h1>
                    <p class="text-gray-600 mb-2"><strong>Director:</strong>
                        <%= singleMovie.director %>
                    </p>
                    <p class="text-gray-600 mb-2"><strong>Release Year:</strong>
                        <%= singleMovie.releaseYear %>
                    </p>
                    <p class="text-gray-600 mb-2"><strong>Genre:</strong>
                        <%= singleMovie.genre %>
                    </p>
                    <p class="text-gray-700 mt-4">
                        <%= singleMovie.description %>
                    </p>
                </div>
            </div>
        </div>
    </div>

</body>

</html>