<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Subcategories</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100">
    <!-- Include Header -->
    <%- include('../header') %>

        <div class="flex">
            <!-- Include Sidebar -->
            <%- include('../sidebar') %>

                <!-- Main Content -->
                <div class="ml-64 flex-1 p-8">
                    <!-- Page Header -->
                    <div class="mb-6 flex justify-between items-center">
                        <h2 class="text-2xl font-bold text-gray-800">Subcategories</h2>
                        <a href="/admin/addSubCategoryPage"
                            class="bg-indigo-600 text-white px-4 py-2 rounded-md hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                            Add Subcategory
                        </a>
                    </div>

                    <!-- Subcategories Table -->
                    <div class="bg-white shadow-md rounded-lg">
                        <table class="min-w-full bg-white border border-gray-200 rounded-lg">
                            <thead>
                                <tr class="bg-gray-100 text-gray-600 uppercase text-lg leading-normal">
                                    <th class="py-3 px-6 text-left">#</th>
                                    <th class="py-3 px-6 text-left">Category Name</th>
                                    <th class="py-3 px-6 text-left">Subcategory Name</th>
                                    <th class="py-3 px-6 text-center">Actions</th>
                                </tr>
                            </thead>
                            <tbody class="text-gray-900 font-bold text-lg  font-light">
                                <% SubCategories.forEach((subCategory, index)=> { %>
                                    <tr class="border-b border-gray-200 hover:bg-gray-100">
                                        <td class="py-3 px-6 text-left">
                                            <%= index + 1 %>
                                        </td>
                                        <td class="py-3 px-6 text-left">
                                            <%= subCategory.categoryId ? subCategory.categoryId.categoryName : "N/A" %>
                                        </td>
                                        <td class="py-3 px-6 text-left">
                                            <%= subCategory.subCategoryName %>
                                        </td>
                                        <td class="py-3 px-6 text-center">
                                            <a href="/admin/editSubCategoryPage/<%= subCategory._id %>"
                                                class="bg-blue-500 text-white px-3 py-1 rounded-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-400">
                                                Edit
                                            </a>
                                            <a href="/admin/deleteSubCategory/<%= subCategory._id %>"
                                                class="bg-red-500 ms-4 text-white px-3 py-1 rounded-md hover:bg-red-600 focus:outline-none focus:ring-2 focus:ring-red-400">
                                            Delete
                                            </a>
                                        </td>
                                    </tr>
                                    <% }) %>
                            </tbody>
                        </table>
                    </div>
                </div>
        </div>
</body>

</html>